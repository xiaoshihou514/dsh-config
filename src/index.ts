/** Personal DSH configuration bundle root; the browser discovers this package. */

export const name = "dsh-config";

export function apply(): void {
  // The host-side accounting route is mounted by ./usage-api.
}
